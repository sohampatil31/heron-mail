# Used by the `alembic` command line tool during development.
# The app itself runs migrations from code (see src/heron/core/migrate.py).
[alembic]
script_location = src/heron/migrations
file_template = %%(rev)s_%%(slug)s
